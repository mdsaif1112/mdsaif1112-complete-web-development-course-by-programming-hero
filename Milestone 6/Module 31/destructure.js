// Object Destructure

const fish = {
    name: 'Ilish',
    boyosh: 5,
    dam: 5000
}

const {name, boyosh, dam} = fish;

// console.log(name, boyosh, dam);

const numbers = [45, 75, 86, 90, 99]

const [math, physics, english, bangla, ict] = numbers

console.log(ict);